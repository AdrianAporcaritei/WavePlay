spring.application.name=com.GarajM.WavePlay

server.port=8080

# PostgreSQL connection
spring.datasource.url=jdbc:postgresql://localhost:5433/WavePlay
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT CONFIG
# =========================
jwt.secret=${JWT_SECRET}
#30 min
jwt.access.token.expiration=1800000
#7days
jwt.refresh.token.expiration=604800000

# =========================
# FILE STORAGE
# =========================
file.storage.song.path=uploads/songs
file.storage.image.path=uploads/images


# =========================
# BASE URL
# =========================
app.base.url=http://localhost:8080


# =========================
# MULTIPART (FILE UPLOAD)
# =========================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB


# =========================
# EMAIL (SMTP - GMAIL EXAMPLE)
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

app.frontend.url=http://localhost:4200

gemini.api.key=AIzaSyBs1CnrwMBxDNi5lnP0QXuxrajiSCYT38Y

gemini.models=gemini-2.5-flash-lite,gemini-2.0-flash-lite,gemini-2.5-flash,gemini-2.0-flash-exp

